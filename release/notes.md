im2agent v2026.0906.0007

源代码提交：4a6daee0c9835b5270608db4d072e309d8390605

## 更新内容

- 飞书回复下方的预览按钮统一显示“预览：标题”；较长的中文或表情标题按字符安全截断，保留前缀和简短省略号。
- 修复飞书转发文本缺少 Codex 桌面端所需的 `text_elements` 字段、可能导致任务页面报错无法查看的问题；普通消息和执行中的补充消息均使用兼容格式，并保留原有文本元素。
- 保持图片、附件路径等消息字段不变。

已出现该渲染错误的任务可能仍保留旧格式的桌面内存缓存。升级中转服务并等当前任务结束后，请完整退出、重新打开 Codex 再验证显示；本次升级不会修改或删除历史记录。

Windows amd64、macOS Intel 和 Apple Silicon 使用同一版本号；macOS 压缩包保留可执行权限。
